{
  "name": "pi-omarchy-sudo-prompt",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Visible, explained privilege requests for Pi on Omarchy",
  "keywords": [
    "pi-package"
  ],
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/adrunkhuman/pi-omarchy-sudo-prompt.git"
  },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "check": "npm test"
  },
  "pi": {
    "extensions": [
      "./extension/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  }
}
